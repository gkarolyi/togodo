package tests

import (
	"testing"
)

// TestAddWithDate tests that adding tasks includes creation date
// Ported from: t1010-add-date.sh
func TestAddWithDate(t *testing.T) {
	t.Skip("TODO: Implement date support - adding tasks should optionally include creation date")

	// env := SetupTestEnv(t)
	//
	// output, code := env.RunCommand("add", "task with date")
	// // With date support enabled, output should include: "YYYY-MM-DD task with date"
	// // This depends on configuration setting for auto-dating
}
